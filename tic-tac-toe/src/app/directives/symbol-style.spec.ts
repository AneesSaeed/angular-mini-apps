import { SymbolStyle } from './symbol-style';

describe('SymbolStyle', () => {
  it('should create an instance', () => {
    const directive = new SymbolStyle();
    expect(directive).toBeTruthy();
  });
});
